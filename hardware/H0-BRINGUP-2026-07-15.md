# Core2 H0 bring-up evidence — 2026-07-15

## Scope

Read-only board identification and full factory-flash backup. No erase, flash,
rollback write, eFuse action, Wi-Fi onboarding or private identifier capture was
authorized or performed.

## Physical factory baseline

- Board enclosure and USB-C data connection: `PASS`.
- Factory firmware: `v2.4`, build identifier `20250703`.
- Factory firmware reached its demo screen without a reset loop: `PASS`.
- Factory display, touch response and built-in speaker output: `PASS` as a
  pre-flash baseline only; these results do not qualify the project firmware.
- The factory AWS demo reported the optional ATECC608 device as unavailable and
  allowed local skip. This is recorded as factory-demo behavior, not as a
  project-firmware failure.

## Device identification

- Serial adapter: one unambiguous USB serial candidate; identifier redacted.
- Target: `ESP32-D0WDQ6-V3`, revision `v3.1`.
- Features reported: Wi-Fi, Bluetooth, dual core, 240 MHz.
- Crystal: `40 MHz`.
- Detected flash: `16 MB`, 3.3 V.
- Device MAC and USB serial identifiers are intentionally omitted.

## Recovery evidence

- Full factory read range: `0x00000000` through `0x00ffffff`.
- Backup size: `16,777,216` bytes (`16 MiB`): `PASS`.
- SHA-256 checksum file created and verified locally: `PASS`.
- Backup and checksum remain under ignored `hardware/backups/` with private file
  permissions. The digest is not copied into public project evidence.
- Guarded rollback path requires the same complete backup, checksum verification,
  the confirmed `/dev/cu.*` port and separate `CONFIRM_ROLLBACK=YES`: reviewed.

## Verdict

`H0 PASS — READY TO REQUEST THE SEPARATE H1 FIRST-FLASH GATE`

This dated H0 result preceded the current exact-image registry. Future writes
must build without a device action, record the reviewed candidate in
`hardware/approved-app-images.json`, and use
`scripts/core2-device-action.sh flash-image` after explicit confirmation of the
exact SHA-256. Direct build-and-flash shortcuts are disabled.
